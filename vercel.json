{
  "framework": "angular",
  "buildCommand": "pnpm i --frozen-lockfile && pnpm nx build ng-host",
  "outputDirectory": "dist/ng-host",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
